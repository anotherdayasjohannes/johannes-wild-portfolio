/**
 * Root layout - REQUIRED by Next.js App Router
 * Just provides <html>, locale layout provides <body>
 */
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

